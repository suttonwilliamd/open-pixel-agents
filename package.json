{
  "name": "pixel-agents-opencode",
  "displayName": "Pixel Agents - OpenCode",
  "description": "Pixel art office where your OpenCode agents come to life as animated characters",
  "version": "1.0.0",
  "publisher": "your-name",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/pixel-agents-opencode"
  },
  "icon": "icon.png",
  "license": "MIT",
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pixel-agents-opencode.showPanel",
        "title": "Pixel Agents (OpenCode): Show Panel"
      },
      {
        "command": "pixel-agents-opencode.exportDefaultLayout",
        "title": "Pixel Agents (OpenCode): Export Layout as Default"
      }
    ],
    "viewsContainers": {
      "panel": [
        {
          "id": "pixel-agents-opencode-panel",
          "title": "Pixel Agents (OpenCode)",
          "icon": "$(window)"
        }
      ]
    },
    "views": {
      "pixel-agents-opencode-panel": [
        {
          "type": "webview",
          "id": "pixel-agents-opencode.panelView",
          "name": "Pixel Agents (OpenCode)"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "build:webview": "cd webview-ui && npm run build",
    "compile": "npm run check-types && npm run lint && node esbuild.js && npm run build:webview",
    "build": "npm run compile",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production && npm run build:webview",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "import-tileset": "tsx scripts/import-tileset-cli.ts"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "@types/node": "22.x",
    "@types/pngjs": "^6.0.5",
    "@types/vscode": "^1.107.0",
    "esbuild": "^0.27.2",
    "eslint": "^9.39.2",
    "npm-run-all": "^4.1.5",
    "pngjs": "^7.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0"
  }
}
